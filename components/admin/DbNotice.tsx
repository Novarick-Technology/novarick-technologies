export function DbNotice() {
  return (
    <div className="rounded-panel bg-paper-muted px-4 py-3 font-body text-[13px] text-text-body">
      Database isn&rsquo;t connected yet — showing sample data so you can click through the interface. Nothing
      below is real, and changes here won&rsquo;t be saved until it&rsquo;s wired up.
    </div>
  );
}
